/*
  Root helper file.
  This project uses server.js as the main backend for the WebUI.
  app.js is included here so the package feels complete and to avoid confusion.
  Running node app.js is equivalent to running node server.js.
*/
require("./server");
